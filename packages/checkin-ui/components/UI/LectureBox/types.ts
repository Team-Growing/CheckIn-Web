export interface LectureBoxProps {
  title: string;
  grade: string;
  place: string;
  teacher: string;
  people: string;
}

export interface EnrolLectureBoxProps {
  title: string;
  grade: string;
  place: string;
  people: string;
}
